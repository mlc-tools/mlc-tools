не работает list<enum> сериализация. Варианты:
    - пойти по примеру map и для листа использовать сериализацию конкретного типа (предпочтительно)
    - добавить list<enum> в протоколы для всех форматов (нежелательно)